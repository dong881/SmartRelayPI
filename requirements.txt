Flask>=3.0.0 # Update Flask to a newer version
RPi.GPIO==0.7.1
Flask-SQLAlchemy>=3.0.0 # Update Flask-SQLAlchemy
python-dotenv==0.19.0
# Let pip resolve compatible versions for these based on Flask:
# Werkzeug
# Jinja2
# itsdangerous
# click
# MarkupSafe
# SQLAlchemy (handled by Flask-SQLAlchemy)
# greenlet (handled by SQLAlchemy)
# typing-extensions (handled by SQLAlchemy)